/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponseTaskDto } from '../../models/api-response-task-dto';
import { AssignTaskRequest } from '../../models/assign-task-request';

export interface AssignTask$Params {
  taskId: string;
  Authorization: string;
      body: AssignTaskRequest
}

export function assignTask(http: HttpClient, rootUrl: string, params: AssignTask$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseTaskDto>> {
  const rb = new RequestBuilder(rootUrl, assignTask.PATH, 'put');
  if (params) {
    rb.path('taskId', params.taskId, {});
    rb.header('Authorization', params.Authorization, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseTaskDto>;
    })
  );
}

assignTask.PATH = '/api/tasks/{taskId}/assign';
