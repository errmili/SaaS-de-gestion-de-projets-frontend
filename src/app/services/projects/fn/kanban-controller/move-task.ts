/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponseVoid } from '../../models/api-response-void';

export interface MoveTask$Params {
  taskId: string;
  newStatus: string;
  position?: number;
  Authorization: string;
}

export function moveTask(http: HttpClient, rootUrl: string, params: MoveTask$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseVoid>> {
  const rb = new RequestBuilder(rootUrl, moveTask.PATH, 'put');
  if (params) {
    rb.path('taskId', params.taskId, {});
    rb.query('newStatus', params.newStatus, {});
    rb.query('position', params.position, {});
    rb.header('Authorization', params.Authorization, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseVoid>;
    })
  );
}

moveTask.PATH = '/api/kanban/tasks/{taskId}/move';
