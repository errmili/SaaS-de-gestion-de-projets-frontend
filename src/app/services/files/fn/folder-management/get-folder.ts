/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponseFolderDto } from '../../models/api-response-folder-dto';

export interface GetFolder$Params {
  folderId: string;
  Authorization: string;
}

export function getFolder(http: HttpClient, rootUrl: string, params: GetFolder$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseFolderDto>> {
  const rb = new RequestBuilder(rootUrl, getFolder.PATH, 'get');
  if (params) {
    rb.path('folderId', params.folderId, {});
    rb.header('Authorization', params.Authorization, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseFolderDto>;
    })
  );
}

getFolder.PATH = '/api/folders/{folderId}';
